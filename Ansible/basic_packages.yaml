---
- hosts: all
  tasks:
    - name: Install basic packages
      ansible.builtin.package:
        name:
          - tree
          - parted
          - vim
          - git
          - epel-release
          - ansible
        state: latest